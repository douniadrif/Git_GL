
public class point {
	int x ;
	int y ;
	public point(int x, int y) {
	this.x = x; this.y = y;
	}
	public static boolean identiques(point a, point b) {
		return ((a.x == b.x) && (a.y == b.y)) ;
		
	public boolean identique (point a)  {
		return ((a.x == x) && (a.y == y)); 
	
	private int x, y;
	}
	
	public class PointDonnées extends point {
	private char nom;
	private String couleur;
	PointDonnées(int x, int y, char nom, String couleur) {
	}
	
	}
	￼

	super(x, y);
	this.nom = nom; this.couleur = couleur;
	}
	
}



